<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <receiver
            android:name=".PluginDiscoveryReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="io.nightfish.lightnovelreader.PLUGIN_DISCOVERY" />
            </intent-filter>
        </receiver>

        <meta-data android:name="lnr_web_data_source" android:value="com.guangyu.plugin.GuangYuWebDataSource" tools:node="merge"/>
    </application>
</manifest>

